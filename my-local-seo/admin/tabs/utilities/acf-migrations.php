<?php
/**
 * Utilities view stub.
 *
 * IMPORTANT:
 * The MYLS admin tab loader recursively includes every PHP file under /admin/tabs.
 * This file MUST NOT output anything directly, or it will render above the MYLS
 * interface and also block the real Utilities page (because include_once would
 * prevent it from being required again).
 *
 * The real Utilities UI lives at:
 *   admin/views/utilities-acf-migrations.php
 */
if ( ! defined('ABSPATH') ) exit;
// Intentionally empty.
